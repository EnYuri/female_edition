/**
 * Type-only stub for `src/api/api.types`.
 *
 * The original module declared nothing but types, so it produced no runtime code and
 * the bundler never emitted it — which means the published sourcemap has no copy of it
 * and the real declarations are unrecoverable. Importers still name these symbols, and
 * esbuild keeps an import it cannot prove is type-only, so the module has to resolve.
 *
 * Types are erased before execution, so `any` here changes nothing at runtime; it only
 * makes type checking permissive.
 */

export type ActorItemSectionCommand = any;
export type ActorTabRegistrationOptions = any;
export type ContentRegistrationOptions = any;
export type CustomContentInjectParams = any;
export type CustomHeaderControlsEntry = any;
export type EquipmentTypeGroup = any;
export type ExistingTabAssociationOptions = any;
export type HeaderControlRegistrationParams = any;
export type ItemSummaryCommand = any;
export type ItemTabRegistrationOptions = any;
export type OnContentReadyParams = any;
export type OnRenderParams = any;
export type RenderScheme = any;
export type SheetHeaderControlPosition = any;
export type SupportedContent = any;
export type SupportedTab = any;
export type TabIdDocumentItemTypesOptions = any;
export type TabIdDocumentItemTypesParams = any;
export type UseSpecificLevelExhaustionParams = any;
