/**
 * Type-only stub for `src/types/item.types`.
 *
 * The original module declared nothing but types, so it produced no runtime code and
 * the bundler never emitted it — which means the published sourcemap has no copy of it
 * and the real declarations are unrecoverable. Importers still name these symbols, and
 * esbuild keeps an import it cannot prove is type-only, so the module has to resolve.
 *
 * Types are erased before execution, so `any` here changes nothing at runtime; it only
 * makes type checking permissive.
 */

export type AdvancementItemContext = any;
export type AdvancementSectionContext = any;
export type AdvancementsContext = any;
export type ContainerContents = any;
export type ContainerItemContext = any;
export type ContainerSection = any;
export type ContainerSheetClassicContext = any;
export type ContainerSheetQuadroneContext = any;
export type CurrencyContext = any;
export type Item5e = any;
export type ItemChatData = any;
export type ItemDescription = any;
export type ItemFacilityOrdersContext = any;
export type ItemSheetContext = any;
export type ItemSheetQuadroneContext = any;
export type MovementInfo = any;
export type SenseInfo = any;
export type UsesRecoveryData = any;
