/**
 * Type-only stub for `src/foundry/TidyFlags.types`.
 *
 * The original module declared nothing but types, so it produced no runtime code and
 * the bundler never emitted it — which means the published sourcemap has no copy of it
 * and the real declarations are unrecoverable. Importers still name these symbols, and
 * esbuild keeps an import it cannot prove is type-only, so the module has to resolve.
 *
 * Types are erased before execution, so `any` here changes nothing at runtime; it only
 * makes type checking permissive.
 */

export type AttributeItemPinFlag = any;
export type AttributePinFlag = any;
export type DocumentJournalEntries = any;
export type DocumentJournalEntry = any;
export type EncounterCombatantSettings = any;
export type EncounterCombatantsSettings = any;
export type EncounterPlaceholder = any;
export type EncounterPlaceholders = any;
export type SheetItemPinFlag = any;
export type SheetPinFlag = any;
export type TidyFlagNamedNotes = any;
export type TidyFlagUnnamedNotes = any;
